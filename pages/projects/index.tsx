import React from 'react'
import ProjectsContainer from '@/components/ProjectsContainer'
import RenderContainer from '@/components/RenderContainer'

const Projects = () => {
  return (
    <RenderContainer title="Projects">
      <ProjectsContainer />
    </RenderContainer>
  )
}

export default Projects
